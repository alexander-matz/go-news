<html>
<!-- vim: ts=2 sts=2 sw=2 et ai
-->
<head>
  <title>news</title>
  <link rel="stylesheet" href="{{.base}}/static/style.css">
</head>
<body>
  <div id="#content">

    {{ $base := .base }}
    <h1>control center</h1>
    <p> Stats </p>
    <ul>
      {{ range $key, $val := .stats }}
        <li>
        {{ $key }} - {{ $val }}
        </li>
      {{ end }}
    </ul>

  </div>
</body>
</html>
